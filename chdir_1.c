#include<stdio.h>
#include<errno.h>
#include<unistd.h>
int main()
{
    char Path[50]; 
    getcwd(path,sizeof(Path); 
   printf(" current directire %s\n",Path);
   chdir(".Data"); 
   printf(" current directire %s\n",Path); //error in that f

   open(sync_1.c);
   
    return 0; 
}
